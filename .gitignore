# macOS
.DS_Store

# Backups created by bootstrap.sh during adoption / deployment
*.bak.*

# Never any secrets here (the perimeter is deliberately "methodology only")
.env
*.local
.credentials.json

# Claude Code session artifacts (out of perimeter — no settings/caches/sessions)
.claude/
